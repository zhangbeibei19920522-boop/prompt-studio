# Prompt Manager - 项目设计文档

> 创建日期：2026-02-28
> 最后更新：2026-02-28
> 状态：设计完成

## 项目概述

Prompt Manager 是一个本地部署的 **Agent 驱动的对话式 Prompt 管理平台**。用户通过对话方式与 AI Agent 交互，Agent 根据项目的业务说明、业务目标、业务背景及知识库，智能地生成、修改和优化 prompt。

平台的核心价值：**不是让用户手动写 prompt，而是让 Agent 理解业务后帮用户写 prompt。**

## 目标用户

- **当前阶段**：公司内部团队（产品、运营、业务人员）
- **远期目标**：发展为 SaaS 产品，面向更广泛用户

## 核心概念

| 概念 | 说明 |
|------|------|
| **全局设置** | AI 模型配置 + 全局业务说明/目标/背景 |
| **项目 (Project)** | 一组相关 prompt 的容器，有自己的业务说明/目标/背景、知识库 |
| **Prompt** | 项目内的 prompt 模板，有版本历史，可附带自身的补充说明 |
| **知识库 (Knowledge Base)** | 项目级别，包含上传的业务文档和已有 prompt 参考 |
| **会话 (Session)** | 项目内的一次对话，Agent 在会话中与用户交互完成 prompt 任务 |

### 三级业务信息架构

```
全局（Global）
├── 业务说明：适用于所有项目的通用规则
├── 业务目标：组织层面的 prompt 使用目标
└── 业务背景：整体业务背景

项目（Project）
├── 业务说明：该项目特有的规则（含强制写入 prompt 的规则）
├── 业务目标：该项目的 prompt 要解决的问题
└── 业务背景：该项目的 prompt 是怎么应用的

Prompt（单条）
└── 补充说明：该 prompt 的具体用途、注意事项
```

Agent 在每次生成/修改 prompt 时，**必须读取三级业务信息**，确保产出内容符合业务要求。

## 用户主流程

```
1. 创建项目
   ↓
2. 维护项目信息
   ├── 业务说明（含强制规则）
   ├── 业务目标
   └── 业务背景
   ↓
3. 上传知识库（可选）
   ├── 业务文档（PDF / Word / TXT / Markdown）
   └── 已有 prompt 文件
   ↓
4. 发起会话（新建对话）
   ↓
5. 用户在对话框中：
   ├── @ 引用一个或多个已有 prompt（如有）
   ├── @ 引用一个或多个知识库文档（如有）
   └── 输入需求描述
   ↓
6. Agent 规划阶段
   ├── 读取全局 / 项目 / prompt 三级业务信息
   ├── 读取相关知识库内容
   ├── 拆分需求为关键点
   ├── 规划每个关键点对应的 prompt 操作（新建 / 修改）
   └── 以「规划卡片」展示给用户确认
   ↓
7. 用户确认规划 → Agent 执行
   ├── 新建 prompt → 全文预览 + 可编辑
   └── 修改 prompt → diff 对比 + 可编辑
   ↓
8. 用户反馈 → Agent 二次修改（可循环）
   ├── Agent 每次修改前重新读取三级业务信息 + 知识库
   └── 直到用户满意
   ↓
9. 用户确认 → prompt 保存（自动生成版本记录）
```

## 页面布局（对话中心化架构）

### 整体布局：三栏结构

```
┌──────────────────────────────────────────────────────────┐
│  顶部栏：[项目选择下拉] [+ 新项目]        [全局设置⚙]     │
├────────────┬─────────────────────────┬───────────────────┤
│ 左侧栏      │  中央：对话区域           │ 右侧面板（可折叠） │
│ (240px)     │                         │ (400px)           │
│             │                         │                   │
│ ┌─────────┐│  ┌─────────────────┐    │ 根据上下文切换：    │
│ │会话列表   ││  │ Agent 消息       │    │                   │
│ │- 会话 1  ││  │ 规划卡片         │    │ · Prompt 全文预览  │
│ │- 会话 2  ││  │ Prompt 预览/Diff │    │ · Diff 对比视图    │
│ │- + 新会话 ││  │ 用户消息         │    │ · 可编辑区域       │
│ └─────────┘│  │ ...              │    │ · 知识库文档预览   │
│             │  └─────────────────┘    │                   │
│ ┌─────────┐│                         │                   │
│ │项目资源   ││  ┌─────────────────┐    │                   │
│ │📋 Prompt ││  │ 输入框            │    │                   │
│ │  - prompt1││  │ [@ 引用] [发送]   │    │                   │
│ │  - prompt2││  └─────────────────┘    │                   │
│ │📁 知识库  ││                         │                   │
│ │  - doc1  ││                         │                   │
│ │📂 其他项目││                         │                   │
│ │⚙ 项目设置 ││                         │                   │
│ └─────────┘│                         │                   │
└────────────┴─────────────────────────┴───────────────────┘
```

### 对话区域中的特殊组件

**1. 规划卡片**（Agent 规划后展示）

```
┌─────────────────────────────────────┐
│ 📋 修改规划                          │
│                                     │
│ 根据您的需求，我拆分为以下关键点：       │
│                                     │
│ ✅ 1. 修改「欢迎语 prompt」            │
│    → 增加多语言支持逻辑                │
│ ✅ 2. 新建「语言检测 prompt」           │
│    → 自动识别用户语言                  │
│ ☐ 3. 修改「回复模板 prompt」           │
│    → 适配多语言输出格式                │
│                                     │
│         [确认执行]  [修改规划]          │
└─────────────────────────────────────┘
```

**2. Prompt 预览卡片**（新建 prompt 时）

```
┌─────────────────────────────────────┐
│ ✨ 新建 Prompt：语言检测 prompt        │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ (完整 prompt 内容预览)            │ │
│ │ 你是一个语言检测助手...            │ │
│ └─────────────────────────────────┘ │
│                                     │
│   [应用保存]  [在右侧编辑]  [拒绝]    │
└─────────────────────────────────────┘
```

**3. Diff 卡片**（修改已有 prompt 时）

```
┌─────────────────────────────────────┐
│ ✏️ 修改 Prompt：欢迎语 prompt         │
│                                     │
│ - 你是一个客服助手，请用中文回复       │
│ + 你是一个客服助手，请用{{语言}}回复   │
│                                     │
│   [应用修改]  [在右侧编辑]  [拒绝]    │
└─────────────────────────────────────┘
```

**4. @ 引用交互**

- 输入 `@` 时弹出下拉列表
- 分两组：**Prompt**（当前项目 + 其他项目只读）和 **知识库文档**
- 支持连续多次 @ 选择多个引用
- 选中后以标签形式嵌入输入框：`[@欢迎语 prompt] [@回复模板 prompt] [@产品手册.pdf] 请优化多语言支持`
- 标签可点击 x 移除

### 页面结构（完整信息架构）

```
全局设置页
├── AI 模型配置
├── 全局业务说明 / 目标 / 背景
└── 导入 / 导出

项目列表 / 切换
└── 项目内部（三栏布局）
    ├── 左侧栏
    │   ├── 会话列表（+ 新建会话）
    │   ├── 项目资源
    │   │   ├── Prompt 列表
    │   │   ├── 知识库文档
    │   │   └── 项目设置（业务信息维护）
    │   └── 其他项目（只读引用）
    ├── 中央：对话区域
    │   ├── 消息流（用户消息 + Agent 消息）
    │   ├── 规划卡片
    │   ├── Prompt 预览 / Diff 卡片
    │   └── 输入框（支持 @ 多选引用）
    └── 右侧面板（上下文相关）
        ├── Prompt 全文预览 / 编辑
        ├── Diff 对比视图
        └── 知识库文档预览
```

## Agent 工作流

### 每次用户发消息时的处理流程

```
用户发送消息（含 @ 引用 + 需求描述）
         ↓
┌─────────────────────────────┐
│  1. 收集上下文               │
│  ├── 全局业务信息             │
│  ├── 当前项目业务信息          │
│  ├── @ 引用的 prompt（含各自说明）│
│  ├── @ 引用的知识库文档        │
│  └── 当前会话历史记录          │
└─────────────────────────────┘
         ↓
┌─────────────────────────────┐
│  2. 规划                     │
│  ├── 分析需求，拆分关键点      │
│  ├── 确定每个关键点的操作       │
│  │   （新建 / 修改哪些 prompt） │
│  └── 生成「规划卡片」→ 用户确认  │
└─────────────────────────────┘
         ↓ 用户确认
┌─────────────────────────────┐
│  3. 执行                     │
│  ├── 逐个处理每个关键点        │
│  ├── 调用 LLM 生成/修改 prompt │
│  └── 输出预览卡片 / Diff 卡片   │
└─────────────────────────────┘
         ↓
┌─────────────────────────────┐
│  4. 迭代                     │
│  ├── 用户反馈 → 重新收集上下文  │
│  ├── Agent 二次修改            │
│  └── 循环直到用户满意          │
└─────────────────────────────┘
         ↓ 用户确认应用
┌─────────────────────────────┐
│  5. 保存                     │
│  ├── 写入 prompt（新版本号）    │
│  └── 记录版本历史              │
└─────────────────────────────┘
```

### Agent System Prompt 构造策略

Agent 每次调用 LLM 时，按以下顺序拼装上下文：

```
1. System Prompt（Agent 角色定义 + 行为规则）
2. 全局业务信息（说明 + 目标 + 背景）
3. 项目业务信息（说明 + 目标 + 背景）
4. 被引用的 Prompt 内容（含各自的补充说明）
5. 被引用的知识库文档内容
6. 当前会话历史（最近 N 条消息）
7. 用户当前消息
```

## 技术方案

### 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 前端 | Next.js + React + TypeScript | 现代化 UI 框架 |
| UI 组件 | Tailwind CSS + shadcn/ui | 美观且开发效率高 |
| 后端 API | Next.js API Routes | 前后端一体，部署简单 |
| 数据库 | SQLite (better-sqlite3) | 本地部署，零配置 |
| 向量搜索 | 轻量向量存储方案 | 知识库语义检索 |
| AI 接口 | 用户自配 | 支持多家大模型（见下方） |
| 流式输出 | Server-Sent Events (SSE) | Agent 对话实时流式响应 |
| 分发方式 | npm 包 / npx | 一行命令安装启动 |

### 支持的 AI 模型

| 厂商 | 模型示例 | API 兼容性 |
|------|---------|-----------|
| OpenAI | GPT-4o, GPT-4o-mini | OpenAI 原生 API |
| Anthropic | Claude 3.5 Sonnet, Claude 4 | Anthropic API |
| 月之暗面 | Kimi (moonshot-v1-8k 等) | OpenAI 兼容 API |
| 智谱 AI | GLM-4, GLM-4-Air | OpenAI 兼容 API |
| DeepSeek | DeepSeek-Chat, DeepSeek-Coder | OpenAI 兼容 API |
| 通义千问 | Qwen-Max, Qwen-Plus | OpenAI 兼容 API |
| 其他 | 任何 OpenAI 兼容 API | 自定义 Base URL |

> 大部分国内模型都兼容 OpenAI API 格式，只需用户配置对应的 Base URL 和 API Key 即可。

### 选择理由

1. **一个项目搞定前后端**，维护简单
2. **npx prompt-manager** 一行命令即可安装启动
3. JS/TS 对接各家 AI API 非常方便
4. 后续转 SaaS 最容易 —— Next.js 天然支持云端部署
5. SQLite 本地存储，用户数据完全在自己手上

## 数据模型

### GlobalSettings（全局设置）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| provider | string | AI 服务商 |
| apiKey | string (加密) | API Key |
| model | string | 模型名称 |
| baseUrl | string | API 端点 |
| businessDescription | text | 全局业务说明 |
| businessGoal | text | 全局业务目标 |
| businessBackground | text | 全局业务背景 |

### Project（项目）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| name | string | 项目名称 |
| description | text | 项目简介 |
| businessDescription | text | 项目级业务说明（含强制规则） |
| businessGoal | text | 项目级业务目标 |
| businessBackground | text | 项目级业务背景 |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

### Prompt

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| projectId | string | 所属项目 |
| title | string | 标题 |
| content | text | Prompt 内容 |
| description | text | 补充说明（用途、注意事项） |
| tags | json | 标签 |
| variables | json | 变量定义 |
| version | number | 当前版本号 |
| status | enum | draft / active / archived |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 更新时间 |

### PromptVersion（版本历史）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| promptId | string | 所属 Prompt |
| version | number | 版本号 |
| content | text | 该版本的内容 |
| changeNote | text | 修改说明（Agent 自动生成） |
| sessionId | string | 产生该版本的会话 |
| createdAt | datetime | 创建时间 |

### Document（知识库文档）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| projectId | string | 所属项目 |
| name | string | 文件名 |
| type | string | 文件类型（pdf / docx / txt / md） |
| content | text | 解析后的文本内容 |
| embedding | vector | 向量化数据 |
| createdAt | datetime | 创建时间 |

### Session（对话会话）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| projectId | string | 所属项目 |
| title | string | 会话标题（Agent 自动命名） |
| createdAt | datetime | 创建时间 |
| updatedAt | datetime | 最后活跃时间 |

### Message（消息）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 唯一标识 |
| sessionId | string | 所属会话 |
| role | enum | user / assistant |
| content | text | 消息内容 |
| references | json | @ 引用的 prompt/文档 ID 列表 |
| metadata | json | 规划卡片、diff 等结构化数据 |
| createdAt | datetime | 创建时间 |

## 部署方式

```bash
# 安装并启动（目标体验）
npx prompt-manager

# 或全局安装
npm install -g prompt-manager
prompt-manager start
```

启动后浏览器自动打开 `http://localhost:3000`

## AI 模型配置

用户在全局设置页面中配置：
- Model Provider（选择厂商或自定义）
- API Key
- Model 名称
- Base URL（支持自定义端点，国内模型必填）
